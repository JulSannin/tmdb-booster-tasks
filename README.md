# TMDB Booster

Веб-приложение для поиска и просмотра информации о фильмах на данных [TMDB](https://www.themoviedb.org/).

## Задание

- [Описание задания](1-description.md) — требования к приложению
- [Система оценки](2-grading-system.md) — критерии и баллы

---

## Стек

| Технология                | Назначение                              |
| ------------------------- | --------------------------------------- |
| React 19 + TypeScript     | основа приложения                       |
| Vite                      | сборка и сервер разработки              |
| Redux Toolkit + RTK Query | состояние и запросы к API               |
| React Router              | маршрутизация, режим фреймворка         |
| CSS Modules               | стили, встроены в Vite (`*.module.css`) |
| ESLint + Prettier         | проверка и форматирование кода          |
| steiger                   | проверка структуры проекта по FSD       |

---

## Запуск локально

### 1. Установить зависимости

```bash
npm install
```

### 2. Добавить ключ TMDB

Создать файл `.env.local` в корне проекта:

```
VITE_TMDB_API_KEY=ваш_ключ
```

Префикс `VITE_` обязателен — без него переменная не попадёт в приложение.

Как получить ключ — в [описании задания](1-description.md).

### 3. Запустить

```bash
npm run dev
```

Приложение откроется на http://localhost:5173

---

## Команды

| Команда           | Что делает                                 |
| ----------------- | ------------------------------------------ |
| `npm run dev`     | сервер разработки                          |
| `npm run build`   | проверка типов и сборка в `build/`         |
| `npm run preview` | локальный просмотр собранной версии        |
| `npm run lint`    | проверка кода линтером                     |
| `npm run format`  | форматирует все файлы Prettier             |
| `npm run fsd`     | следит за структурой папок по правилам FSD |

---

## Структура проекта

Проект построен по методологии [Feature-Sliced Design](https://feature-sliced.design/). Слои упорядочены, импорты разрешены только вниз:

```
app       запуск приложения
pages     страницы
widgets   крупные самостоятельные блоки
features  действия пользователя
entities  сущности предметной области
shared    переиспользуемое без привязки к теме проекта
```

Слой заводится, когда в нём появляется код. Сейчас их три:

```
src/
├── app/
│   ├── model/store/       хранилище Redux
│   ├── routing/
│   │   ├── root.tsx       оболочка страницы, провайдер Redux
│   │   ├── routes/        файлы маршрутов: отдают страницы React Router
│   │   └── routes.ts      список маршрутов
│   └── styles/
│       └── global.css     глобальные стили
├── pages/                 страницы
├── shared/
│   ├── api/               запросы к TMDB
│   └── config/            адреса TMDB и переменные окружения
└── vite-env.d.ts          типы переменных окружения
```

Точку входа создаёт React Router: папка приложения задана в `react-router.config.ts`, маршруты — в `routes.ts`.

Соблюдение правил проверяется автоматически: `npm run fsd`.

Импорты внутри `src` пишутся через алиас `@`:

```ts
import { TMDB_BASE_URL } from '@/shared/config';
```
